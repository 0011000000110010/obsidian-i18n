{
    "manifest": {
        "author": "Fendi",
        "version": "1.0.0"
    },
    "dict": {
        "throw new TypeError(\"Class extends value \" + String(b) + \" is not a constructor or null\");": "throw new TypeError(\"类扩展值 \" + String(b) + \" 不是构造函数或 null\");",
        "if (f) throw new TypeError(\"Generator is already executing.\");": "if (f) throw new TypeError(\"生成器已在执行。\");",
        "throw new Error('No active view found');": "throw new Error('未找到活动视图');",
        ".setName('Shortcuts')": ".setName('文本扩展')",
        ".setName('Shell executable')": ".setName('Shell可执行文件')",
        ".setDesc('All commands will be executed inside it.')": ".setDesc('所有命令都将在其中执行。')",
        "descEl.appendText('Are defined as a JSON-list. Fields:');": "descEl.appendText('被定义为JSON列表。字段:');",
        "descEl.appendText(' (required) - trigger pattern');": "descEl.appendText('（必需）-触发器模式');",
        "descEl.appendText(' (optional) - text replacement, used if provided');": "descEl.appendText('（可选）-文本替换，如果提供，则使用');",
        "descEl.appendText(' (optional) - shell command whose stdout is used as a replacement');": "descEl.appendText('（可选）- 标准输出用作替代的 shell 命令');"
    }
}
